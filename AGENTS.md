# mp8090-hitlist プロジェクトルール

## 作業範囲と開始前の確認

このリポジトリは複数の端末（自宅Mac・会社WindowsPC）およびエディタから編集されることがあります。

読み取り専用の依頼では、ファイル変更、`git pull`、更新スクリプトの実行、`docs/decisions.md` への追記を行わないこと。

編集作業を始める前に、Gitルート、現在のbranch、upstream、作業ツリーの状態を確認する。編集作業では原則として以下を実行する。現在のリモートとローカルが同一commitであることを別の読み取り手段で確認できた場合だけ、省略理由を報告したうえで省略できる。

    git pull --ff-only

`git pull --ff-only` が失敗した場合、履歴が分岐している場合、または既存変更と編集対象が競合する場合は、自動解決せず作業を停止して状況を報告すること。

- 同期や編集のために `git reset`、自動stash、checkoutによる既存変更の破棄を行わない
- 未追跡ファイル、とくに `images/setuplits/` の画像を、依頼と無関係なのに一括で追加・削除・変更しない
- 無関係な未追跡ファイルは保持し、作業対象と明確に分ける
- commit・pushは、ユーザーから明示された作業範囲に含まれる場合だけ実行する

### なぜこれが必須か（実際に起きた事故）

2026年7月1日、#294（KATSUMI特集）のデータを追加した直後、別のカレンダー修正作業を「pull前の古いローカルコピー」に対して行ってしまい、その修正をコミット・プッシュした結果、#294のデータが上書きで消失する事故が発生した。
git status 上は「origin/main と同期済み」と表示されていたが、これはコミット時点のローカルとリモートが一致していただけであり、その中身自体に既に不足があったため、一見「同期済みなのにデータが無い」という矛盾した状態が生まれた。

### 作業開始時のチェックリスト

1. Gitルート、branch、upstream、`git status` を確認する
2. 編集対象と競合する既存変更がないことを確認する
3. 原則 `git pull --ff-only` を実行し、編集対象ファイル（setuplits.html, index.html 等）が最新版であることを確認する。省略する場合はリモートとの一致を確認して理由を報告する
4. commit・pushまで依頼されている場合は、編集・コミット後に `git push` し、可能であれば GitHub上のファイル（生のcommits/mainページ）で反映を目視確認する

## NEW_EPISODE_NO / NEW_EPISODE_DATE の同期

新しい放送回を追加する際、`NEW_EPISODE_NO`（新着として表示する放送回番号）は `setuplits.html` と `index.html` の両方で手動同期する。

`NEW_EPISODE_DATE` は `index.html` のみにある**サイト反映日**であり、放送日ではない。新着表示を更新した日として手動更新すること。

新着回はデータ中の最大回番号とは限らない。`NEW_EPISODE_NO` を最大回番号へ機械的に置き換えず、今回サイトへ反映する回を確認して設定すること。

## 原表記を保持する

曲名・歌手名・備考などの原表記を一括正規化しない。検索や初登場判定に必要な正規化は、原データを書き換えず照合時だけ行う。

誤記を修正するときは正式表記などの根拠を確認し、対象回、曲番号、フィールド、想定変更件数を限定する。変更後は検索・初登場判定などへの影響も確認し、重要な判断理由を `docs/decisions.md` に記録する。

## 画像ファイルの命名規則

新しい回の画像は **新形式 `PUY年.回番号.拡張子`** で統一する（例：`1995.300.png` / `1985.293.png`）。

- ユーザーから届く画像は `YYYY_回番号.拡張子`（アンダースコア）であることが多い。**必ずドット区切りにリネームしてから** commit すること
- 画像を追加するときは `git add` にパスを明示する。`images/setuplits/` には未追跡のアンダースコア画像が溜まっているため、一括 add すると未配線のまま巻き込む
- `image_file` に書いた名前と実ファイル名は1文字でもズレると本番で404になる。追加後は実在チェックすること

**#279 以前の回には旧形式（`PUY年.放送年.MMDD.回番号3桁.拡張子`）など複数の形式が混在しているが、これは正常な状態である。**
「変則名」「命名ミス」と判断して直さないこと。混在の全内訳と、一括リネームを行わないと決めた理由は
[docs/decisions.md](docs/decisions.md) を参照。

## データの単一情報源（写しを作らない）

Set Up List の本体データは **`setuplits.html` 内の `SETUPLIST_DATA`**（`// <<SETUPLIST_DATA_START>>` 〜 `// <<SETUPLIST_DATA_END>>` マーカー間）ただ一つである。
**ここ以外にデータの写し（コピー・エクスポート・キャッシュ）を作らないこと。**

### なぜこれが必須か（実際に起きた事故の構造）

かつて `data/setuplits.json` という写しが存在したが、初回コミット以降まったく更新されず、
本体が157回更新される間に **181回分／曲データ0件** という大きく乖離した状態のまま放置されていた（2026年8月26日に削除）。
写しが存在すると「どちらが正か」が曖昧になり、古い側で本体を上書きした瞬間に #294 と同種のデータ消失事故が起きる。

## 使用禁止スクリプト

`scripts/export_setuplits_json.py` は **使用禁止**。ファイル冒頭に `sys.exit` によるガードを入れてあるので、そのまま実行しても停止する。

このスクリプトは SQLite DB から `episode_no` / `puy_year` / `air_date` / `special_feature` のみを取得し、
`tracks` を一切含まず `image_file` を無条件 `None` にしたデータで、`setuplits.html` のマーカー間を**まるごと上書き**する作りになっている。
実行すれば全曲データと画像参照が消える。冒頭のガードコメントを読まずに外さないこと。

通常作業でこの停止ガードを解除してはならない。

## 取込・更新スクリプトの扱い

スクリプトを実行する前に、入力元、対象ファイル、上書き範囲、外部依存を確認すること。

- Excel取込は、既存回に曲が登録済みの場合の曲修正、画像の配線、`NEW_EPISODE_NO` / `NEW_EPISODE_DATE` の更新まで自動完結しない。取込後にそれぞれを確認する
- ランキングのスクレイピング結果は、年別件数、欠落、取得失敗を確認してから採用する。取得失敗で空配列になった年を含む結果で既存の `data.json` をそのまま上書きしない
- 過去の特定回を対象にした変換・修正・パッチスクリプトを、通常の更新へ流用しない

## 変更後の検証

変更後は対象ファイルの差分を確認し、意図しない回・曲の削除、画像参照切れ、対象外差分がないことを確認する。

- データ件数は固定値を合格条件にせず、変更前後で意図した増減になっているか確認する
- 欠番、`image_file: null`、旧形式の画像名を、異常と決めつけて自動修復しない
- 変更内容に応じて、検索、`?ep=NNN` の回指定リンク、モーダル、カレンダー、共通CSSを利用する `index.html` / `setuplits.html` / `year-end-chart.html` / `gallery.html` を確認する
- 仕様が未確定な挙動は独断で変更せず、現状維持または状況報告を優先する
- 実施した確認と未確認事項を報告し、成果物は別担当のreviewerで確認する

## ルールと決定事項の管理

当面は `CLAUDE.md` と `AGENTS.md` を同時に更新し、更新後に内容が一致することを確認する。両方をGitで追跡し、Claude CodeとCodexのどちらから作業しても同じルールが適用される状態を保つ。

新しい重要な判断理由は `docs/decisions.md` に記録する。ただし、読み取り専用の調査では追記しない。

## 参照ゼロだが削除禁止のファイル

以下は他のどのファイルからも参照されていないが、**削除してはいけない**。

| ファイル | 理由 |
|---|---|
| `top.html` | 旧URL救済用のリダイレクトページ。`index.html` へ meta refresh する。直リンクで到達されるため参照ゼロが正常 |
| `googleba0f6ee57d1ea362.html` | Google Search Console の所有権確認ファイル。削除すると検索流入の計測が停止する |

「参照ゼロだから不要」という判断は、この2ファイルには適用しないこと。
